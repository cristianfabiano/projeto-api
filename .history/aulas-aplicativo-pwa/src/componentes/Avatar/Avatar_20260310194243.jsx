import "./Avatar.css";

const Avatar = ({nome}) => {
    const firstLetter = nome.split('').filter(word => word).map(word =>[])
    console.log(nome);
    return <div></div>

}

export default Avatar;